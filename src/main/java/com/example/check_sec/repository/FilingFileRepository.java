package com.example.check_sec.repository;

import com.example.check_sec.entity.FilingFile;
import org.springframework.data.jpa.repository.JpaRepository;

public interface FilingFileRepository extends JpaRepository<FilingFile, Long> {
}
